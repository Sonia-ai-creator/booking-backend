specVersion: "v1beta"
serviceId: "booking-backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "booking-backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
